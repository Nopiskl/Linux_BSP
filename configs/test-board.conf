#!/bin/bash
# Test Board Configuration
# Test configuration for BSP framework

BOARD_NAME="test-board"
DEVICE_DTS="test/test-board"
ARCH="arm64"
KERNEL_BRANCH="bsp"
KERNEL_GCC="aarch64-linux-gnu-"
BOOTARGS="root=LABEL=rootfs rootwait rootfstype=ext4 console=ttyS0,115200"

case "${KERNEL_TARGET}" in
    bsp)
        LINUX_REPO="https://github.com/torvalds/linux.git"
        LINUX_GITEE_REPO=""
        LINUX_BRANCH="v6.1"
        LINUX_CONFIG="defconfig"
        LINUX_PATHDIR="none"
        ;;
esac

setup_dhcp(){
    echo "Test board setup_dhcp"
}

BL_CONFIG="sunxi-uboot"
UBOOT_REPO="https://github.com/u-boot/u-boot.git"
UBOOT_BRANCH="v2023.10"
ATF_REPO="https://github.com/ARM-software/arm-trusted-firmware.git"
ATF_BRANCH="lts-v2.10.4"
BL_CONF="test_board_defconfig"
BL_PATCHDIR="test-board"
